
# 📚 Итоговые инструкции по резервному копированию (Утилита `tar`)

Мы будем использовать несжатый формат `.tar`, так как он позволяет легко добавлять (дописывать) файлы в существующий архив.

## 1\. 💾 Создание нового архива (Бэкап)

Эта команда создает новый архив, сохраняя **абсолютный путь** к файлу, что позволяет восстановить его точно в оригинальное местоположение.

  * **Назначение:** Создать новый архив, содержащий файлы и каталоги, с сохранением их полной структуры пути.
  * **Используемая опция:** **`-c`** (create).

### Пример 1: Создание архива с нуля

Создать архив `full_backup.tar`, включающий `/etc/hosts` и весь каталог `/var/log`.

```bash
tar -cvf ~/full_backup.tar /etc/hosts /var/log
```

| Опция | Значение |
| :--- | :--- |
| **`-c`** | **C**reate (Создать новый архив). |
| **`-v`** | **V**erbose (Подробный вывод). |
| **`-f`** | **F**ile (Имя файла архива). |
| **Пути** | Файлы и каталоги, которые нужно добавить (с полными путями). |

-----

## 2\. ➕ Добавление файлов в существующий архив

Эта команда дописывает новые файлы в конец уже созданного несжатого архива.

  * **Назначение:** Добавить один или несколько файлов в существующий архив, не перезаписывая его.
  * **Используемая опция:** **`-r`** (append).

### Пример 2: Добавление файла в архив

Добавить новый файл `/home/client/config.txt` в ранее созданный `full_backup.tar`.

```bash
tar -rvf full_backup.tar /home/client/config.txt
```

-----

## 3\. 🔍 Просмотр содержимого и путей

Эта команда позволяет увидеть список всех файлов в архиве и их сохраненные пути, не распаковывая архив.

  * **Назначение:** Вывести список содержимого архива.
  * **Используемая опция:** **`-t`** (list).

### Пример 3: Просмотр содержимого

```bash
tar -tf full_backup.tar
```

**Вывод:** (Показывает сохраненные пути)

```
etc/hosts
var/log/
var/log/secure
...
home/client/config.txt
```

-----

## 4\. 📤 Извлечение конкретного файла (Восстановление)

Эта команда извлекает только указанный файл из архива, не затрагивая остальные.

  * **Назначение:** Извлечь один файл или каталог.
  * **Используемая опция:** **`-x`** (extract).

### Пример 4: Извлечение одного файла

Извлечь только файл `etc/hosts` в **текущий каталог**.

```bash
tar -xvf full_backup.tar etc/hosts
```

*(Будет создан каталог `./etc/` и в нем файл `hosts`)*

### 💡 Расширение: Восстановление в оригинальное место

Чтобы восстановить файл точно в его оригинальное местоположение (например, `/etc/hosts`), используйте опцию **`-C`** для смены каталога на корень (`/`) перед извлечением:

```bash
sudo tar -xvf full_backup.tar -C / etc/hosts
```

-----

## 5\. ⭐️ Полезные дополнительные команды

### 5.1. Исключение файлов или каталогов

Используйте опцию **`--exclude`** для игнорирования определенных файлов или шаблонов во время создания архива.

  * **Сценарий:** Создать архив `/var/log`, но исключить все файлы `.tmp`.
    ```bash
    tar -cvf log_backup.tar --exclude='*.tmp' /var/log
    ```

### 5.2. Сжатие архива (для экономии места)

Если место важнее скорости добавления файлов, вы можете сжать архив. Для этого используйте опцию **`-z`** (gzip) при создании или просмотре.

  * **Создание сжатого архива (.tar.gz):**
    ```bash
    tar -czvf compressed_backup.tar.gz /home/user/data
    ```
  * **Просмотр сжатого архива:**
    ```bash
    tar -tzf compressed_backup.tar.gz
    ```
    *(Не забывайте, что в сжатый архив нельзя добавить файлы с помощью `-r`)*

### 5.3. Создание бэкапа только измененных файлов

Если вы хотите создать инкрементный бэкап (архивировать только те файлы, которые были изменены после определенной даты), используйте опцию **`--newer`** (или `--newer-mtime`).

  * **Сценарий:** Архивировать файлы, измененные после 1 января 2025 года.
    ```bash
    tar -cvf recent_files.tar --newer='2025-01-01' /home/client
    ```

-----

Это основные и расширенные действия, которые покроют большинство ваших потребностей в резервном копировании с сохранением пути.

Хотели бы вы рассмотреть какой-нибудь из этих расширенных сценариев более подробно?
