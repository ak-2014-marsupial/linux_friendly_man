
##  Ограничения прав доступа к скриптам и unit-файлам в Linux, а также способы решения этих проблем.

### 1. 🛑 Блокировка запуска скрипта другого пользователя

| Причина блокировки | Суть проблемы | Способы обхода (Решения) |
| :--- | :--- | :--- |
| **Недостаточные права доступа** | У вашего пользователя (или его группы) **нет права на исполнение (`x`)** и/или **чтение (`r`)** исходного файла скрипта. | 1. **Изменить права:** Владелец или `root` должен выполнить `chmod +x /путь/к/скрипту` (или `chmod 755`). |
| **Конфликт владельцев** | Вы не можете запустить скрипт, потому что он принадлежит другому пользователю, и ваш пользователь не имеет необходимых прав. | 2. **Копирование:** Скопировать файл в свою директорию (`cp`). Владельцем копии становитесь **вы**, и вы можете свободно установить права `chmod +x` для запуска. |
| **Требуются повышенные права** | Скрипт содержит команды, требующие прав `root` (например, перезапуск службы, запись в `/etc`). | 3. **Использование `sudo`:** Запустить скрипт от имени другого пользователя или `root` с помощью `sudo`: `sudo -u владелец /путь/к/скрипту`. |

---

### 2. 🚫 Блокировка при копировании файла

| Причина блокировки | Суть проблемы | Способы обхода (Решения) |
| :--- | :--- | :--- |
| **Нет права на запись в целевую папку** | У вашего пользователя **нет права на запись (`w`)** в ту директорию (папку), куда вы пытаетесь скопировать файл (например, `/home/user/`). | 1. **Восстановить права:** Убедиться, что целевая директория принадлежит вам, и восстановить права на запись: `chmod u+w /путь/к/директории`. |
| **Использование `sudo cp`** | Если вы используете `sudo cp`, файл копируется с правами `root`, даже если целевая папка принадлежит вам. | 2. **Смена владельца:** После копирования немедленно сменить владельца копии на себя: `sudo chown ваш_пользователь:ваша_группа /путь/к/файлу`. |

---

### 3. ⚙️ Ограничения для файлов systemd

Unit-файлы (`.service`) — это **системные конфигурационные файлы**, и к ним предъявляются более строгие требования безопасности, чтобы предотвратить компрометацию системы.

#### А. Ограничения на **Скрипт** (файл, который запускается через `ExecStart`)

| Ограничение | Суть требования | Последствия при нарушении |
| :--- | :--- | :--- |
| **Право на исполнение** | Скрипт должен иметь право **`x`** для пользователя, указанного в директиве `User=`. Рекомендуется **`755`**. | `systemd` не сможет запустить файл, возникнет ошибка. |
| **Ограничения безопасности** | Unit-файл может включать директивы (`PrivateTmp=`, `ProtectSystem=`), которые **ограничивают** возможности скрипта. | Скрипт может работать неправильно, если пытается получить доступ к запрещенным системным ресурсам. |

#### Б. Ограничения на **Unit-файл** (например, `/etc/systemd/system/my.service`)

| Ограничение | Суть требования | Последствия при нарушении |
| :--- | :--- | :--- |
| **Владелец и группа** | Должен принадлежать пользователю **`root`** и группе **`root`**. | `systemd` может выдать предупреждение или игнорировать файл. |
| **Права доступа** | Должен быть **защищен от записи** кем-либо, кроме `root`. Рекомендуемые права: **`644`** (`-rw-r--r--`). | Угроза безопасности: неавторизованный пользователь может изменить конфигурацию сервиса и выполнить код с правами `root`. |

**Способ обхода/решения:** Для работы с unit-файлами всегда используйте **`sudo`** и создавайте их в каталоге `/etc/systemd/system/`. Убедитесь, что после создания права установлены как `644`.
