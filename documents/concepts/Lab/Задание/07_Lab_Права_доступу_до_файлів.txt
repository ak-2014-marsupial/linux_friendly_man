Пункт 1. підготовка
Зайдіть в систему під користувачем creator.

Створіть директорію /tmp/dir з такими правами доступу rwx------

Створіть файл file в директорії /tmp/dir з такими правами доступу rw-------

Зайдіть в директорію /tmp/dir перегляньте вміст в довгому форматі, перевірте правильність виконання завдання.



Пункт 2. право на читання для директорій
Приберіть права на читання з директорії /tmp/dir

Перегляньте права доступу директорії /tmp/dir ( використайте параметр -d для ls ). Вийшло?

Перегляньте права доступу файлу file. Вдалося?

Перейдіть в директорію /tmp/dir. Вийшло?

Перегляньте вміст директорії /tmp/dir.  Вдалося?

Створіть файл /tmp/dir/file2. Вийшло?

Зробіть висновки про вплив права на читання для директорії

Поверніть налаштування прав доступу в початковий стан та перейдіть в домашню директорію.


Пункт 3. право на виконання для директорії
Перевірте, що права доступу файлу та директорії знаходяться в початковому стані і перебуваючи в домашньому каталозі (~)

Приберіть право на виконання з директорії /tmp/dir

Перегляньте права доступа директорії /tmp/dir ( використовуйте параметр -d для ls ). Вийшло?

Перегляньте права доступу файлу file. Вдалося?

Перейдіть в директорію /tmp/dir. Вийшло?

Переглянути вміст директорії /tmp/dir. Вдалося?

Створіть файл /tmp/dir/file3. Вийшло?

Зробіть висновок про вплив права на виконання для директорії.

Поверніть налаштування прав доступу в початковий стан.


Пункт 4. Право на запис для директорії
Перевірте, що права доступу файлу та директорії знаходяться в початковому стані і перебуваючи в домашньому каталозі (~)

Приберіть права на запис з директорії /tmp/dir

Перегляньте права доступу директорії /tmp/dir ( використовуйте параметр -d для ls ). Вийшло?

Перегляньте права доступу файлу file. Вдалося?

Перейдіть в директорію /tmp/dir. Вийшло?

Перегляньте вміст директорії /tmp/dir. Вдалося?

Створіть файл /tmp/dir/file4. Вийшло?

Видаліть файл /tmp/dir/file. Вдалося?

Зробіть висновок про вплив права на запис для директорії.

Поверніть налаштування прав доступу в початковий стан.


Пункт 5. Права групи
[creator] Перевірте, що права доступу файлу та директорії знаходяться в початковому стані.

Зайдіть з другої консолі під користувачем destroyer

[destroyer] Перегляньте права доступу директорії /tmp/dir ( використовуйте параметр -d для ls ). Вийшло?

[destroyer] Перегляньте права доступу файлу file.  Вдалося?

[destroyer] Перейдіть в директорію /tmp/dir. Вийшло?

[destroyer] Перегляньте вміст директорії /tmp/dir. Вдалося?

[destroyer] Створіть файл /tmp/dir/file5. Вийшло?

[destroyer] Видаліть файл /tmp/dir/file. Вдалося?


Зробіть висновок про вплив прав групи для директорії.


[creator] Додайте право на читання файлу /tmp/dir/file для групи

[destroyer] Перегляньте права доступу директорії /tmp/dir ( використовуйте параметр -d для ls ). Вийшло?

[destroyer] Перегляньте права доступу файлу file. Вдалося?

[destroyer] Перейдіть в директорію /tmp/dir. Вийшло?

[destroyer] Перегляньте вміст директорії /tmp/dir. Вдалося?


[creator] Додайте право на читання та виконання директорії  /tmp/dir для групи

[destroyer] Перегляньте права доступу директорії /tmp/dir ( використовуйте параметр -d для ls ). Вийшло?

[destroyer] Перегляньте права доступу файлу file. Вдалося?

[destroyer] Перейдіть в директорію /tmp/dir. Вийшло?

[destroyer] Перегляньте вміст директорії /tmp/dir. Вдалося?


[creator] Додайте право на запис файлу /tmp/dir/file для групи

[destroyer] Створіть файл /tmp/dir/file6. Вийшло?

[destroyer] Видаліть файл /tmp/dir/file. Вдалося?


[creator] Додайте право на запис директорії  /tmp/dir/ для групи

[destroyer] Створіть файл /tmp/dir/file7. Вийшло?

[destroyer] Видаліть файл /tmp/dir/file. Вдалося?

[destroyer] Видаліть всі файли в директорії /tmp/dir/. Вийшло?

[destroyer] Видаліть директорію /tmp/dir/. Вдалося?



Пункт 6. Атрибути файлів
Частина 1

Зайдіть під своїм користувачем в систему.

Перейдіть в режим суперкористувача.

Створіть файл /append_only

Відкрийте man chattr і знайдіть атрибут append only

Додайте атрибут на файл /append_only

Перенаправте результат команди echo "text" в файл /append_only з перезаписом. Вийшло?

Перенаправте результат команди echo "text" в файл /append_only дописуючи в кінець файлу. Вдалося?

Видалити файл /append_only. Вийшло?

Зніміть атрибут з файлу /append_only.

Видалити файл /append_only

Частина 2

Зайдіть під своїм користувачем в систему.

Перейдіть в режим суперкористувача.

Створіть файл /immutable

Відкрийте man chattr і знайдіть атрибут "immutable"

Додайте атрибут на файл /immutable

Перенаправте результат команди echo "text" в файл /immutable з перезаписом. Вийшло?

Перенаправте результат команди echo "text" в файл /immutable дописуючи в кінець файлу. Вдалося?

Видаліть файл /immutable. Вийшло?

Зніміть атрибут з файлу /immutable.

Видаліть файл /immutable


