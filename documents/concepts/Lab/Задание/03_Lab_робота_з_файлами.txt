    1.Запустіть 2 з'єднання з віртуальною машиною Термінал1 і Термінал2

    Створіть робочу директорію lab-files для виконання лабораторної роботи.

    Подальші команди виконуємо в Термінал1 і в цій директорії
    


    2.Створіть файл main-file використовуючи touch або >
    

    3.Створіть символічне посилання link-file на файл main-file, використовуючи  ln, stat, ls

    Перегляньте метаінформацію про файл link-file, main-file (вміст inode)

    Створіть символічне посилання link-dir на директорию lab-files

    Перегляньте метаінформацію про файл link-dir

    

    4.Створіть "жорстке" посилання hardlink-file на файл main-file, використовуючи ln, mkdir, stat, ls

    Перегляньте метаінформацію про файл hardlink-file, main-file (вміст inode)

    Спробуйте створити жорстке посилання hardlink-dir на директорію lab-files

    Зробіть висновки про відмінності символічних та "жорстких" посилань.

   

    5.Запишіть рядок “string1” в файл main-file не використовуючи текстового редактора, використовуючи echo, >, cat, >>, head, tail.

        Перегляньте вміст файлу main-file

        Допишіть рядок string2 в файл main-file не використовуючи текстового редактора.

        Перегляньте вміст файлу main-file

        Допишіть рядок string3 в файл символічного посилання link-file

        Перегляньте вміст файлу main-file

        Допишіть рядок string4 в файл “жорсткого” посилання hardlink-file

        Перегляньте вміст файлу main-file

    Зробіть висновки про те в який блок даних записуються данні при зверненні до файлу напряму, через символічне посилання, через "жорстке" посилання.

        Виведіть тільки перший рядок файлу main-file

        Виведіть тільки останній рядок файлу main-file

 

    6.Перейдіть в Термінал2 і виведіть вміст файлу символічного посилання link-file в інтерактивному режимі. Ви повинні побачити рядки:

    string1
    string2
    string3
    string4

    При цьому термінал "зависає" і в баш не повертається
    


   7. Перейдіть в Термінал1 і допишіть рядок “string5” в файл  “жорсткого” посилання hardlink-file

    Перейдіть в Термінал2 і подивіться чи вивівся рядок “string5”?

    8.Знайти в man mount опцію монтування в режимі read-only, використовуючи підрядок запиту (substring) read.Викоритайте  ls, man, grep, |

    Знайти в man mount опцію монтування без збереження access time, використовуючи підрядок запиту access, а потім підрядок time


    Знайти в директорії /etc/ файли які мають слово "host" в імені (не залежно від регістру)

    Знайти в директорії /etc/ (включно з підкаталогами) всі файли, у вмісті яких зустрічаються 127.0.0.1

   


   9. Знайдіть картинки (*.jpg і *.png)  та збережіть список файлів-картинок у файл pics-file. Використайте find, >, >>, file, |, sort

    Перегляньте дані заголовку (метаінформацію) будь-якого файла із списку в файлі pics-file


    Знайдіть архіви (*.zip і *.gz) та збережіть список файлів-архівів у файл arch-file

    Перегляньте дані заголовку (метаінформацію) будь-якого файла із списку в файлі arch-file


    Знайдіть файли, які належать вашому користувачу та збережіть у файл my-files.

    Відсортуйте файли (в виводі та в файлі) за ім'ям

    


    10.Розмір файлів (використайте find, >, 2>, du, |, sort, head, &>): 

        Знайдіть файли, розмір яких більше 20МБ.

        Перенаправте потік похибок попередньої команди в файл find.err, щоб не заважав виводу файлів

        Виведіть топ-5 найбільших файлів в файл find.log 
        Визначте в межах всього дерева які файли не займають корисний простір на жорсткому диску.


  

    Додатково:

    * Ще раз запустіть команду пошуку файлів, але перенаправте список файлів і потік похибок в один файл find.full_log

    ** Ще раз запустіть команду пошуку файлів, але перенаправте список файлів у файл /root/find.log, а потік похибок у файл /root/find.err

Примітка: завдання ** слід виконувати не від користувача root, а від користувача padavan який теж є в системі. 
